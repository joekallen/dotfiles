{
    "sourceFile": "dot_config/nushell/scripts/git/worktree.nu",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 23,
            "patches": [
                {
                    "date": 1746543978696,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1746544322323,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,7 @@\n use common.nu\n use utils.nu\n-use std/dirs shells-aliases\n+use std/dirs shells-aliases enter\n \n # Creates a new worktree from the current branch\n # and will automatically move into the new worktree\n @category git\n@@ -73,8 +73,9 @@\n export def --env git-worktree-use [\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n+    \n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n     if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n"
                },
                {
                    "date": 1746544348110,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -73,9 +73,9 @@\n export def --env git-worktree-use [\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n-    \n+    if\n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n     if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n"
                },
                {
                    "date": 1746544355498,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -73,9 +73,9 @@\n export def --env git-worktree-use [\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n-    if\n+    if ($worktree | is-empty) {\n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n     if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n"
                },
                {
                    "date": 1746544361232,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -73,9 +73,9 @@\n export def --env git-worktree-use [\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n-    if ($worktree | is-empty) {\n+    if ($worktree | is-empty) { return \n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n     if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n"
                },
                {
                    "date": 1746544371211,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -73,9 +73,10 @@\n export def --env git-worktree-use [\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n-    if ($worktree | is-empty) { return \n+    if ($worktree | is-empty) {\n+      \n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n     if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n"
                },
                {
                    "date": 1746544376415,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -74,9 +74,11 @@\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n     if ($worktree | is-empty) {\n-      \n+\n+    }\n+\n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n     if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n"
                },
                {
                    "date": 1746544399778,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -74,9 +74,9 @@\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n     if ($worktree | is-empty) {\n-\n+      git-branch-root | enter\n     }\n \n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n"
                },
                {
                    "date": 1746544407615,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -74,9 +74,9 @@\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n     if ($worktree | is-empty) {\n-      git-branch-root | enter\n+      git-branch-root | enter | \n     }\n \n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n"
                },
                {
                    "date": 1746544455835,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -74,15 +74,16 @@\n   worktree?: string        # The name of the new worktree\n   --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n   ] {\n     if ($worktree | is-empty) {\n-      git-branch-root | enter | \n+      git-branch-root | enter | return\n     }\n \n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n-    if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n+    if ($worktrees | is-empty) {\n+      print $'Worktree `($worktree)` not found' }\n     $worktrees\n       | first\n       | if $in.is_current { return (enter $in.path) }\n       | $in.path\n"
                },
                {
                    "date": 1746544491735,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -81,9 +81,11 @@\n     let worktrees = git-worktree-list | where name == $worktree\n     let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n \n     if ($worktrees | is-empty) {\n-      print $'Worktree `($worktree)` not found' }\n+      print $'Worktree `($worktree)` not found'\n+      return\n+    }\n     $worktrees\n       | first\n       | if $in.is_current { return (enter $in.path) }\n       | $in.path\n"
                },
                {
                    "date": 1746544527801,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -84,8 +84,9 @@\n     if ($worktrees | is-empty) {\n       print $'Worktree `($worktree)` not found'\n       return\n     }\n+\n     $worktrees\n       | first\n       | if $in.is_current { return (enter $in.path) }\n       | $in.path\n"
                },
                {
                    "date": 1746544547599,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -88,8 +88,9 @@\n \n     $worktrees\n       | first\n       | if $in.is_current { return (enter $in.path) }\n+      | print $in\n       | $in.path\n       | if $keep_path { path join $relative_path }\n       | expand\n       | enter\n"
                },
                {
                    "date": 1746544578766,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -87,8 +87,9 @@\n     }\n \n     $worktrees\n       | first\n+\n       | if $in.is_current { return (enter $in.path) }\n       | print $in\n       | $in.path\n       | if $keep_path { path join $relative_path }\n"
                },
                {
                    "date": 1746544587066,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,97 @@\n+use common.nu\n+use utils.nu\n+use std/dirs shells-aliases enter\n+\n+# Creates a new worktree from the current branch\n+# and will automatically move into the new worktree\n+@category git\n+export def --env --wrapped git-worktree-add [\n+  worktree: string         # The name of the new worktree\n+  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n+  --delete (-d) = false    # Whether to delete the worktree if it already exists\n+  ...rest                  # Optional arguments to pass along to the git worktree add command\n+]: nothing -> nothing {\n+  let span = (metadata $worktree).span\n+\n+  let worktrees = git-worktree-list\n+\n+  $worktrees | where name == $worktree\n+    | if ($in | is-not-empty) {\n+        first\n+        | if $delete { git-worktree-remove $in.name }\n+          else if (agree $'Worktree `($in.name)` already exists locally, would you like to use it instead?') {\n+            return (git-worktree-use $in.name --keep-path=$keep_path)\n+          } else if (agree $'Worktree `($in.name)` already exists at `($in.path)`, would you like to delete it?') {\n+            git-worktree-remove $in.name\n+          } else {\n+            error make {\n+              msg: 'Worktree already exists locally'\n+              label: {\n+                span: $span\n+                text: $'($worktree) already exists at `($in.path)`'\n+              }\n+              help: $'Run `git-worktree-use ($in.name)` and verify if you want to use or delete the existing worktree'\n+            }\n+          }\n+      }\n+\n+  let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n+\n+  $worktrees | get-default-branch\n+    | if (not $in.is_current) and (agree $'Not on default branch `($in.name)`, switch now?') {\n+        cd (git-worktree-path $in.name)\n+      }\n+\n+  let worktree_path = git-worktree-path $worktree\n+  ^git worktree add $worktree_path ...$rest | complete | print $in.stdout\n+\n+  $worktree_path | if $relative_path { path join $relative_path } | expand | enter\n+}\n+\n+# Removes a worktree and will automatically move into the default branch\n+@category git\n+export def --env git-worktree-remove [\n+  worktree: string\n+  --force (-f) = false\n+] {\n+  let worktrees = git-worktree-list\n+\n+  $worktrees | where name == $worktree\n+    | if ($in | is-empty) { return (print $'Worktree `($worktree)` not found') }\n+    | ($worktrees | git-current-branch)\n+        | if ($in.name) == $worktree {\n+            enter $in.path\n+          }\n+\n+  if $force { '-f '}\n+    | ^git worktree remove $worktree $in | complete | print $in.stdout\n+}\n+\n+# Switches to a worktree\n+# If already on the worktree, will move to the root of it\n+@category git\n+export def --env git-worktree-use [\n+  worktree?: string        # The name of the new worktree\n+  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n+  ] {\n+    if ($worktree | is-empty) {\n+      git-branch-root | enter | return\n+    }\n+\n+    let worktrees = git-worktree-list | where name == $worktree\n+    let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n+\n+    if ($worktrees | is-empty) {\n+      print $'Worktree `($worktree)` not found'\n+      return\n+    }\n+\n+    $worktrees | first\n+\n+      # | if $in.is_current { return (enter $in.path) }\n+      # | print $in\n+      # | $in.path\n+      # | if $keep_path { path join $relative_path }\n+      # | expand\n+      # | enter\n+}\n"
                },
                {
                    "date": 1746544595681,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -85,8 +85,9 @@\n       print $'Worktree `($worktree)` not found'\n       return\n     }\n \n+    \n     $worktrees | first\n \n       # | if $in.is_current { return (enter $in.path) }\n       # | print $in\n@@ -94,102 +95,4 @@\n       # | if $keep_path { path join $relative_path }\n       # | expand\n       # | enter\n }\n-use common.nu\n-use utils.nu\n-use std/dirs shells-aliases enter\n-\n-# Creates a new worktree from the current branch\n-# and will automatically move into the new worktree\n-@category git\n-export def --env --wrapped git-worktree-add [\n-  worktree: string         # The name of the new worktree\n-  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n-  --delete (-d) = false    # Whether to delete the worktree if it already exists\n-  ...rest                  # Optional arguments to pass along to the git worktree add command\n-]: nothing -> nothing {\n-  let span = (metadata $worktree).span\n-\n-  let worktrees = git-worktree-list\n-\n-  $worktrees | where name == $worktree\n-    | if ($in | is-not-empty) {\n-        first\n-        | if $delete { git-worktree-remove $in.name }\n-          else if (agree $'Worktree `($in.name)` already exists locally, would you like to use it instead?') {\n-            return (git-worktree-use $in.name --keep-path=$keep_path)\n-          } else if (agree $'Worktree `($in.name)` already exists at `($in.path)`, would you like to delete it?') {\n-            git-worktree-remove $in.name\n-          } else {\n-            error make {\n-              msg: 'Worktree already exists locally'\n-              label: {\n-                span: $span\n-                text: $'($worktree) already exists at `($in.path)`'\n-              }\n-              help: $'Run `git-worktree-use ($in.name)` and verify if you want to use or delete the existing worktree'\n-            }\n-          }\n-      }\n-\n-  let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n-\n-  $worktrees | get-default-branch\n-    | if (not $in.is_current) and (agree $'Not on default branch `($in.name)`, switch now?') {\n-        cd (git-worktree-path $in.name)\n-      }\n-\n-  let worktree_path = git-worktree-path $worktree\n-  ^git worktree add $worktree_path ...$rest | complete | print $in.stdout\n-\n-  $worktree_path | if $relative_path { path join $relative_path } | expand | enter\n-}\n-\n-# Removes a worktree and will automatically move into the default branch\n-@category git\n-export def --env git-worktree-remove [\n-  worktree: string\n-  --force (-f) = false\n-] {\n-  let worktrees = git-worktree-list\n-\n-  $worktrees | where name == $worktree\n-    | if ($in | is-empty) { return (print $'Worktree `($worktree)` not found') }\n-    | ($worktrees | git-current-branch)\n-        | if ($in.name) == $worktree {\n-            enter $in.path\n-          }\n-\n-  if $force { '-f '}\n-    | ^git worktree remove $worktree $in | complete | print $in.stdout\n-}\n-\n-# Switches to a worktree\n-# If already on the worktree, will move to the root of it\n-@category git\n-export def --env git-worktree-use [\n-  worktree?: string        # The name of the new worktree\n-  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n-  ] {\n-    if ($worktree | is-empty) {\n-      git-branch-root | enter | return\n-    }\n-\n-    let worktrees = git-worktree-list | where name == $worktree\n-    let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n-\n-    if ($worktrees | is-empty) {\n-      print $'Worktree `($worktree)` not found'\n-      return\n-    }\n-\n-    $worktrees\n-      | first\n-\n-      | if $in.is_current { return (enter $in.path) }\n-      | print $in\n-      | $in.path\n-      | if $keep_path { path join $relative_path }\n-      | expand\n-      | enter\n-}\n"
                },
                {
                    "date": 1746544600830,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -85,9 +85,9 @@\n       print $'Worktree `($worktree)` not found'\n       return\n     }\n \n-    \n+    print $''\n     $worktrees | first\n \n       # | if $in.is_current { return (enter $in.path) }\n       # | print $in\n"
                },
                {
                    "date": 1746544609439,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -85,9 +85,10 @@\n       print $'Worktree `($worktree)` not found'\n       return\n     }\n \n-    print $''\n+    print $'worktrees: ($worktrees)'\n+    print $'worktrees: ($worktrees)'\n     $worktrees | first\n \n       # | if $in.is_current { return (enter $in.path) }\n       # | print $in\n"
                },
                {
                    "date": 1746544614920,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -86,10 +86,10 @@\n       return\n     }\n \n     print $'worktrees: ($worktrees)'\n-    print $'worktrees: ($worktrees)'\n-    $worktrees | first\n+    print $'first: ($worktrees | first)'\n+    # $worktrees | first\n \n       # | if $in.is_current { return (enter $in.path) }\n       # | print $in\n       # | $in.path\n"
                },
                {
                    "date": 1746544638932,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,99 @@\n+use common.nu\n+use utils.nu\n+use std/dirs shells-aliases enter\n+\n+# Creates a new worktree from the current branch\n+# and will automatically move into the new worktree\n+@category git\n+export def --env --wrapped git-worktree-add [\n+  worktree: string         # The name of the new worktree\n+  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n+  --delete (-d) = false    # Whether to delete the worktree if it already exists\n+  ...rest                  # Optional arguments to pass along to the git worktree add command\n+]: nothing -> nothing {\n+  let span = (metadata $worktree).span\n+\n+  let worktrees = git-worktree-list\n+\n+  $worktrees | where name == $worktree\n+    | if ($in | is-not-empty) {\n+        first\n+        | if $delete { git-worktree-remove $in.name }\n+          else if (agree $'Worktree `($in.name)` already exists locally, would you like to use it instead?') {\n+            return (git-worktree-use $in.name --keep-path=$keep_path)\n+          } else if (agree $'Worktree `($in.name)` already exists at `($in.path)`, would you like to delete it?') {\n+            git-worktree-remove $in.name\n+          } else {\n+            error make {\n+              msg: 'Worktree already exists locally'\n+              label: {\n+                span: $span\n+                text: $'($worktree) already exists at `($in.path)`'\n+              }\n+              help: $'Run `git-worktree-use ($in.name)` and verify if you want to use or delete the existing worktree'\n+            }\n+          }\n+      }\n+\n+  let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n+\n+  $worktrees | get-default-branch\n+    | if (not $in.is_current) and (agree $'Not on default branch `($in.name)`, switch now?') {\n+        cd (git-worktree-path $in.name)\n+      }\n+\n+  let worktree_path = git-worktree-path $worktree\n+  ^git worktree add $worktree_path ...$rest | complete | print $in.stdout\n+\n+  $worktree_path | if $relative_path { path join $relative_path } | expand | enter\n+}\n+\n+# Removes a worktree and will automatically move into the default branch\n+@category git\n+export def --env git-worktree-remove [\n+  worktree: string\n+  --force (-f) = false\n+] {\n+  let worktrees = git-worktree-list\n+\n+  $worktrees | where name == $worktree\n+    | if ($in | is-empty) { return (print $'Worktree `($worktree)` not found') }\n+    | ($worktrees | git-current-branch)\n+        | if ($in.name) == $worktree {\n+            enter $in.path\n+          }\n+\n+  if $force { '-f '}\n+    | ^git worktree remove $worktree $in | complete | print $in.stdout\n+}\n+\n+# Switches to a worktree\n+# If already on the worktree, will move to the root of it\n+@category git\n+export def --env git-worktree-use [\n+  worktree?: string        # The name of the new worktree\n+  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n+  ] {\n+    if ($worktree | is-empty) {\n+      git-branch-root | enter | return\n+    }\n+\n+    let worktrees = git-worktree-list | where name == $worktree\n+    let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n+\n+    if ($worktrees | is-empty) {\n+      print $'Worktree `($worktree)` not found'\n+      return\n+    }\n+\n+    print $'worktrees: ($worktrees)'\n+    print $'first: ($worktrees | first)'\n+    $worktrees | first\n+\n+      # | if $in.is_current { return (enter $in.path) }\n+      # | print $in\n+      # | $in.path\n+      # | if $keep_path { path join $relative_path }\n+      # | expand\n+      # | enter\n+}\n"
                },
                {
                    "date": 1746544645990,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -87,112 +87,14 @@\n     }\n \n     print $'worktrees: ($worktrees)'\n     print $'first: ($worktrees | first)'\n-    $worktrees | first\n+    $worktrees\n+      | first\n \n       # | if $in.is_current { return (enter $in.path) }\n       # | print $in\n       # | $in.path\n       # | if $keep_path { path join $relative_path }\n       # | expand\n       # | enter\n }\n-use common.nu\n-use utils.nu\n-use std/dirs shells-aliases enter\n-\n-# Creates a new worktree from the current branch\n-# and will automatically move into the new worktree\n-@category git\n-export def --env --wrapped git-worktree-add [\n-  worktree: string         # The name of the new worktree\n-  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n-  --delete (-d) = false    # Whether to delete the worktree if it already exists\n-  ...rest                  # Optional arguments to pass along to the git worktree add command\n-]: nothing -> nothing {\n-  let span = (metadata $worktree).span\n-\n-  let worktrees = git-worktree-list\n-\n-  $worktrees | where name == $worktree\n-    | if ($in | is-not-empty) {\n-        first\n-        | if $delete { git-worktree-remove $in.name }\n-          else if (agree $'Worktree `($in.name)` already exists locally, would you like to use it instead?') {\n-            return (git-worktree-use $in.name --keep-path=$keep_path)\n-          } else if (agree $'Worktree `($in.name)` already exists at `($in.path)`, would you like to delete it?') {\n-            git-worktree-remove $in.name\n-          } else {\n-            error make {\n-              msg: 'Worktree already exists locally'\n-              label: {\n-                span: $span\n-                text: $'($worktree) already exists at `($in.path)`'\n-              }\n-              help: $'Run `git-worktree-use ($in.name)` and verify if you want to use or delete the existing worktree'\n-            }\n-          }\n-      }\n-\n-  let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n-\n-  $worktrees | get-default-branch\n-    | if (not $in.is_current) and (agree $'Not on default branch `($in.name)`, switch now?') {\n-        cd (git-worktree-path $in.name)\n-      }\n-\n-  let worktree_path = git-worktree-path $worktree\n-  ^git worktree add $worktree_path ...$rest | complete | print $in.stdout\n-\n-  $worktree_path | if $relative_path { path join $relative_path } | expand | enter\n-}\n-\n-# Removes a worktree and will automatically move into the default branch\n-@category git\n-export def --env git-worktree-remove [\n-  worktree: string\n-  --force (-f) = false\n-] {\n-  let worktrees = git-worktree-list\n-\n-  $worktrees | where name == $worktree\n-    | if ($in | is-empty) { return (print $'Worktree `($worktree)` not found') }\n-    | ($worktrees | git-current-branch)\n-        | if ($in.name) == $worktree {\n-            enter $in.path\n-          }\n-\n-  if $force { '-f '}\n-    | ^git worktree remove $worktree $in | complete | print $in.stdout\n-}\n-\n-# Switches to a worktree\n-# If already on the worktree, will move to the root of it\n-@category git\n-export def --env git-worktree-use [\n-  worktree?: string        # The name of the new worktree\n-  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n-  ] {\n-    if ($worktree | is-empty) {\n-      git-branch-root | enter | return\n-    }\n-\n-    let worktrees = git-worktree-list | where name == $worktree\n-    let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n-\n-    if ($worktrees | is-empty) {\n-      print $'Worktree `($worktree)` not found'\n-      return\n-    }\n-\n-    print $'worktrees: ($worktrees)'\n-    print $'first: ($worktrees | first)'\n-    # $worktrees | first\n-\n-      # | if $in.is_current { return (enter $in.path) }\n-      # | print $in\n-      # | $in.path\n-      # | if $keep_path { path join $relative_path }\n-      # | expand\n-      # | enter\n-}\n"
                },
                {
                    "date": 1746544694300,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -89,11 +89,10 @@\n     print $'worktrees: ($worktrees)'\n     print $'first: ($worktrees | first)'\n     $worktrees\n       | first\n-\n-      # | if $in.is_current { return (enter $in.path) }\n-      # | print $in\n+      | if $in.is_current { return (enter $in.path) }\n+      | print $in\n       # | $in.path\n       # | if $keep_path { path join $relative_path }\n       # | expand\n       # | enter\n"
                },
                {
                    "date": 1746544700057,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -90,10 +90,9 @@\n     print $'first: ($worktrees | first)'\n     $worktrees\n       | first\n       | if $in.is_current { return (enter $in.path) }\n-\n-      # | $in.path\n-      # | if $keep_path { path join $relative_path }\n-      # | expand\n-      # | enter\n+      | $in.path\n+      | if $keep_path { path join $relative_path }\n+      | expand\n+      | enter\n }\n"
                },
                {
                    "date": 1746544736745,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,98 @@\n+use common.nu\n+use utils.nu\n+use std/dirs shells-aliases enter\n+\n+# Creates a new worktree from the current branch\n+# and will automatically move into the new worktree\n+@category git\n+export def --env --wrapped git-worktree-add [\n+  worktree: string         # The name of the new worktree\n+  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n+  --delete (-d) = false    # Whether to delete the worktree if it already exists\n+  ...rest                  # Optional arguments to pass along to the git worktree add command\n+]: nothing -> nothing {\n+  let span = (metadata $worktree).span\n+\n+  let worktrees = git-worktree-list\n+\n+  $worktrees | where name == $worktree\n+    | if ($in | is-not-empty) {\n+        first\n+        | if $delete { git-worktree-remove $in.name }\n+          else if (agree $'Worktree `($in.name)` already exists locally, would you like to use it instead?') {\n+            return (git-worktree-use $in.name --keep-path=$keep_path)\n+          } else if (agree $'Worktree `($in.name)` already exists at `($in.path)`, would you like to delete it?') {\n+            git-worktree-remove $in.name\n+          } else {\n+            error make {\n+              msg: 'Worktree already exists locally'\n+              label: {\n+                span: $span\n+                text: $'($worktree) already exists at `($in.path)`'\n+              }\n+              help: $'Run `git-worktree-use ($in.name)` and verify if you want to use or delete the existing worktree'\n+            }\n+          }\n+      }\n+\n+  let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n+\n+  $worktrees | get-default-branch\n+    | if (not $in.is_current) and (agree $'Not on default branch `($in.name)`, switch now?') {\n+        cd (git-worktree-path $in.name)\n+      }\n+\n+  let worktree_path = git-worktree-path $worktree\n+  ^git worktree add $worktree_path ...$rest | complete | print $in.stdout\n+\n+  $worktree_path | if $relative_path { path join $relative_path } | expand | enter\n+}\n+\n+# Removes a worktree and will automatically move into the default branch\n+@category git\n+export def --env git-worktree-remove [\n+  worktree: string\n+  --force (-f) = false\n+] {\n+  let worktrees = git-worktree-list\n+\n+  $worktrees | where name == $worktree\n+    | if ($in | is-empty) { return (print $'Worktree `($worktree)` not found') }\n+    | ($worktrees | git-current-branch)\n+        | if ($in.name) == $worktree {\n+            enter $in.path\n+          }\n+\n+  if $force { '-f '}\n+    | ^git worktree remove $worktree $in | complete | print $in.stdout\n+}\n+\n+# Switches to a worktree\n+# If already on the worktree, will move to the root of it\n+@category git\n+export def --env git-worktree-use [\n+  worktree?: string        # The name of the new worktree\n+  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n+  ] {\n+    if ($worktree | is-empty) {\n+      git-branch-root | enter | return\n+    }\n+\n+    let worktrees = git-worktree-list | where name == $worktree\n+    let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n+\n+    if ($worktrees | is-empty) {\n+      print $'Worktree `($worktree)` not found'\n+      return\n+    }\n+\n+    print $'worktrees: ($worktrees)'\n+    print $'first: ($worktrees | first)'\n+    $worktrees\n+      | first\n+      | if $in.is_current { return (enter $in.path) }\n+      | $in\n+      # | if $keep_path { path join $relative_path }\n+      # | expand\n+      # | enter\n+}\n"
                }
            ],
            "date": 1746543978696,
            "name": "Commit-0",
            "content": "use common.nu\nuse utils.nu\nuse std/dirs shells-aliases\n\n# Creates a new worktree from the current branch\n# and will automatically move into the new worktree\n@category git\nexport def --env --wrapped git-worktree-add [\n  worktree: string         # The name of the new worktree\n  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n  --delete (-d) = false    # Whether to delete the worktree if it already exists\n  ...rest                  # Optional arguments to pass along to the git worktree add command\n]: nothing -> nothing {\n  let span = (metadata $worktree).span\n\n  let worktrees = git-worktree-list\n\n  $worktrees | where name == $worktree\n    | if ($in | is-not-empty) {\n        first\n        | if $delete { git-worktree-remove $in.name }\n          else if (agree $'Worktree `($in.name)` already exists locally, would you like to use it instead?') {\n            return (git-worktree-use $in.name --keep-path=$keep_path)\n          } else if (agree $'Worktree `($in.name)` already exists at `($in.path)`, would you like to delete it?') {\n            git-worktree-remove $in.name\n          } else {\n            error make {\n              msg: 'Worktree already exists locally'\n              label: {\n                span: $span\n                text: $'($worktree) already exists at `($in.path)`'\n              }\n              help: $'Run `git-worktree-use ($in.name)` and verify if you want to use or delete the existing worktree'\n            }\n          }\n      }\n\n  let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n\n  $worktrees | get-default-branch\n    | if (not $in.is_current) and (agree $'Not on default branch `($in.name)`, switch now?') {\n        cd (git-worktree-path $in.name)\n      }\n\n  let worktree_path = git-worktree-path $worktree\n  ^git worktree add $worktree_path ...$rest | complete | print $in.stdout\n\n  $worktree_path | if $relative_path { path join $relative_path } | expand | enter\n}\n\n# Removes a worktree and will automatically move into the default branch\n@category git\nexport def --env git-worktree-remove [\n  worktree: string\n  --force (-f) = false\n] {\n  let worktrees = git-worktree-list\n\n  $worktrees | where name == $worktree\n    | if ($in | is-empty) { return (print $'Worktree `($worktree)` not found') }\n    | ($worktrees | git-current-branch)\n        | if ($in.name) == $worktree {\n            enter $in.path\n          }\n\n  if $force { '-f '}\n    | ^git worktree remove $worktree $in | complete | print $in.stdout\n}\n\n# Switches to a worktree\n# If already on the worktree, will move to the root of it\n@category git\nexport def --env git-worktree-use [\n  worktree?: string        # The name of the new worktree\n  --keep-path (-p) = false # Whether to keep the current relative path when entering the new worktree\n  ] {\n    let worktrees = git-worktree-list | where name == $worktree\n    let relative_path = null | if $keep_path { $env.PWD | path relative-to (git-branch-root) }\n\n    if ($worktrees | is-empty) { print $'Worktree `($worktree)` not found' }\n    $worktrees\n      | first\n      | if $in.is_current { return (enter $in.path) }\n      | $in.path\n      | if $keep_path { path join $relative_path }\n      | expand\n      | enter\n}\n"
        }
    ]
}